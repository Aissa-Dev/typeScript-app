"use strict";
// exemple 01
var greet;
greet = function (name, age) {
    console.log(name + " has " + age + " years old");
};
// exemple 02
var calc;
calc = function (x, y, action) {
    if (action === "add") {
        return x + y;
    }
    else {
        return x - y;
    }
};
// exemple 03
